version: "3.9"
services:
  api:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ALPHA_VANTAGE_KEY=${ALPHA_VANTAGE_KEY:-demo}
